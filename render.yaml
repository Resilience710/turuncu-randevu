# Render.com Blueprint — tek servis: FastAPI (API + statik frontend aynı origin).
# Deploy: Render Dashboard → New → Blueprint → bu repo'yu seç.
services:
  - type: web
    name: turuncu-randevu
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/
    autoDeploy: true
    envVars:
      # --- Supabase (Frankfurt) — lokal .env ile AYNI olmalı ki şifreli veri çözülsün ---
      - key: DATABASE_URL
        sync: false
      - key: PHONE_ENCRYPTION_KEY
        sync: false
      - key: PHONE_HASH_PEPPER
        sync: false
      - key: OTP_SECRET
        sync: false
      # --- Sabitler ---
      - key: OTP_TTL_SECONDS
        value: "300"
      - key: APP_ENV
        value: production
      - key: LOG_LEVEL
        value: INFO
      - key: STATIC_DIR
        value: /app/static
      # --- E-posta / SMTP (Gmail) — OTP + bildirimler. Boşsa OTP ekrana düşer ---
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASSWORD
        sync: false
      - key: SMTP_FROM_EMAIL
        sync: false
      - key: SMTP_FROM_NAME
        value: Turuncu Randevu
      - key: SMTP_USE_TLS
        value: "true"
      # --- VatanSMS (PASİF — kod kenarda, çağrılmıyor) ---
      - key: VATANSMS_API_ID
        sync: false
      - key: VATANSMS_API_KEY
        sync: false
      - key: VATANSMS_SENDER
        sync: false
      - key: VATANSMS_MESSAGE_TYPE
        value: normal
      - key: VATANSMS_BASE_URL
        value: https://api.vatansms.net/api/v1
